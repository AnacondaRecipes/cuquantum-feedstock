mpi:
  - nompi
  - openmpi

# Keep the cuquantum metapackage hash from forking per-mpi flavor; only
# cutensornet/cudensitymat genuinely vary on MPI (encoded in their build string).
ignore_version:
  - mpi
